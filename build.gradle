buildscript {
    repositories {
        google()
        mavenCentral()
    }
    dependencies {
        // Atualizando a versão do Android Gradle Plugin
        classpath 'com.android.tools.build:gradle:7.0.0' // Verifique se esta versão é compatível com seu projeto
    }
}

allprojects {
    repositories {
        google()
        mavenCentral()
    }
}
